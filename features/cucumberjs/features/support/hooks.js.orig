module.exports = function () {
    this.Before(function (callback) {
        this.spooky.start();
        console.log('Spooky initialised');
        callback();
    });

    this.After(function (callback) {
        this.spooky.destroy();
        console.log('Spooky destroyed');
        callback();
    });
};
